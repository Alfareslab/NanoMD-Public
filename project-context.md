# 🧠 ذاكرة مشروع NanoMD

هذا الملف يتابع الحالة الحالية للمشروع والمشاكل المعروفة.

## الحالة الحالية (Current Status)

- الإصدار الحالي: **v1.7.0**
- تم تحديث الترجمة الذكية لاستخدام Gemma 4 عبر Cloudflare Workers AI مع نموذج احتياطي GLM 4.7 Flash.
- تمت إضافة ترجمة واعية بالسياق يمكن تفعيلها أو إيقافها من واجهتي سطح المكتب والموبايل.
- تم رفع حد الترجمة المباشر إلى 50000 حرف.
- تم إصلاح ترجمة النص المحدد داخل المحرر باستخدام مدى التحديد الفعلي.
- دمج زري الترجمة في زر ذكي واحد يدعم الضغطة المطولة للتبديل لتوفير المساحة في الموبايل.
- إصلاحات التكبير التلقائي (Auto-zoom) وإخفاء أشرطة التمرير وتصغير الشعار في واجهة الموبايل.
- تمت إعادة تصميم واجهة الموبايل بالكامل لتشمل شريط أيقونات علوي وشريط أدوات سفلي متكامل مع خيارات تصدير منزلقة (Bottom Sheet).
- تمت إضافة ميزة "الترجمة الذكية" باستخدام Cloudflare Workers AI.
- تم تفعيل ميزة التراجع (Undo) لاستعادة النص الأصلي بعد الترجمة.
- تم تحسين دعم الـ RTL ومعالجة مشكلة تداخل النصوص (Nested Blocks).
- تمت إضافة خاصية نسخ الكود المضمّن (Inline Code Copy).
- تمت إضافة أزرار تنقل ذكية (Smart Scroll Navigation) ومؤشر تقدم رأسي للتمرير.
- تم تفعيل الروابط التلقائية للـ URLs (Auto-link).
- تمت إضافة أداة محاكاة الواجهة (UI Viewport Lab) للمراجعة البصرية دون تعديل كود التطبيق.

## التحديثات الأخيرة (Recent Updates)
*   **v1.7.0 (Current):**
    *   تحديث نموذج الترجمة الأساسي إلى **Gemma 4** (`@cf/google/gemma-4-26b-a4b-it`) بدلاً من Llama 3.1 المتجه للإيقاف.
    *   إضافة نموذج احتياطي **GLM 4.7 Flash** (`@cf/zai-org/glm-4.7-flash`) عند أخطاء الخدمة المؤقتة.
    *   رفع حد الترجمة المباشر إلى **50000 حرف**.
    *   إضافة خيار **ترجمة السياق** لطلب ترجمة طبيعية حسب مجال النص دون عرض التحليل للمستخدم.
    *   إصلاح ترجمة النص المحدد داخل المحرر عبر `selectionStart` و `selectionEnd` بدلاً من الاستبدال النصي العام.
    *   تحديث نافذة **ما الجديد** ورقم الإصدار الظاهر في التطبيق.

*   **2026-04-24:**
    *   إطلاق أداة **UI Viewport Lab** (`ui-viewport-lab.html`) لاختبار الواجهة في مقاسات Desktop و Tablet و Mobile مع دعم حالات المحتوى المختلفة (Empty/Sample).
    *   تصميم الأداة بثيم دافئ (Nescafe/Cream) مع أزرار تحكم باللون اللبني المريح.
    *   دعم تشغيل الأداة والتطبيق محلياً عبر بروتوكول `file://` مع إصلاح مسارات الـ Assets.
    *   تم توثيق تعديل RTL خارجي لواجهة Codex داخل Antigravity في `_Antigravity Pro User/core/codex-rtl-patch/README.md`.
    *   التعديل لا يغير كود تطبيق NanoMD ولا رقم إصداره؛ هو Patch محلي لإضافة `openai.chatgpt` المثبتة في Antigravity.

*   **v1.6.0:**
    *   نقل زر رفع الملفات (`fileInputRef`) ليعمل بشكل صحيح ومستقل عن إخفاء العناصر في الشاشات الصغيرة.
    *   إصلاح شفافية وتصميم مودال "ما الجديد" و "مساعدة" لتعمل بـ Inline Styles مع `Theme CSS Variables` لمنع التداخل مع Tailwind.
    *   إعادة تصميم الهيدر (Header) لسطح المكتب ليكون صفاً واحداً بدون التفاف (`flex-nowrap`).
    *   دعم Markdown في مودال المساعدة وربطه بملف العرض التقديمي `presentation-ar.md`.

## الميزات النشطة (Active Features)

- **Mobile UX Redesign:** واجهة مستخدم مخصصة للموبايل مع شريط أدوات سفلي زجاجي التصميم (Glassmorphism) وقائمة تصدير سفلية (Bottom Sheet) بديلة للزر العائم.
- **Smart Translation:** ترجمة ذكية (AR/EN) تعتمد على Gemma 4 مع fallback إلى GLM 4.7 Flash، وتدعم ترجمة السياق، النص الكامل، النص المحدد داخل المحرر، وميزة التراجع السريع.
- **UI Viewport Lab:** أداة مستقلة لمعاينة الواجهة في مقاسات مختلفة وحالات محتوى متنوعة.
- **Smart Scroll Navigation:** أزرار تنقل ذكية في شريط جانبي مع شريط تقدم التمرير الرأسي.
- **RTL Enhanced:** دعم محسن لاتجاه النص (RTL) للنصوص المختلطة (عربي وإنجليزي) لتجنب تداخل الأقواس والكلمات.
- **Inline Code Copy:** إمكانية نسخ أكواد السطر الواحد بمجرد النقر عليها.
- **Auto-link:** تفعيل النقر على الروابط التلقائية المكتوبة كنص عادي.
- **Print Feature:** إمكانية طباعة المحتوى في شكل مستند نقي بدون أدوات واجهة المستخدم.
- **Cloudflare KV Share:** مشاركة المحتوى عبر رابط قصير مع ميزة الحذف التلقائي بعد 30 يوماً (TTL).
- **Local Draft Save:** حفظ مسودات العمل محلياً في السجل بدون رفعها للانترنت لاسترجاعها لاحقاً بسرعة وخصوصية.
- **Share History:** واجهة لمراجعة الروابط التي تم مشاركتها والمسودات المحلية المحفوظة (مع خاصية استرجاع المسودات).
- **Split View:** عرض المحرر والنتيجة جنباً إلى جنب.
- **Focus Mode:** وضع عدم التشتت المخصص للكتابة.
- **Review Mode:** لتحليل وعرض نصوص Markdown كجداول للمراجعة.
- **Smart Paste:** لصق ذكي وسريع للنصوص.
- **URL Hash Loading:** تحميل محتوى Markdown تلقائياً من رابط مشفر بـ Base64.
- **Split Columns:** فصل عمود البند عن التوصية في جداول المراجعة مع دمجهم عند النسخ.
- **Section Separator:** صفوف فاصلة بصرية بين أقسام جدول المراجعة.
- **Section Colors:** ألوان خلفية متبادلة لكل قسم في جدول المراجعة.
- **File Upload:** رفع ملفات Markdown من الجهاز مباشرة.
- **Manus UI Style:** واجهة بصرية محدثة بخطوط وألوان وأنماط Manus المتميزة.

## المشاكل المعروفة (Known Issues)

- لا توجد مشاكل معلقة أو أخطاء برمجية في الوقت الحالي.

## الخطوات القادمة (Next Steps)

- تنفيذ المرحلة 4 من خطة تحديث الترجمة: الاختبار النهائي والنشر بعد موافقة صريحة من المطور.
- الاستمرار في إضافة قوالب جديدة (Templates) في حال توفرت متطلبات جديدة.
